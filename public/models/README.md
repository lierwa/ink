# ONNX Depth Model

Place the depth ONNX model file here:

- `public/models/depth-anything-v2-small.onnx`

Runtime load path in app: `/models/depth-anything-v2-small.onnx`.

If the file is missing or loading fails, the app falls back to luma-based depth estimation automatically.
